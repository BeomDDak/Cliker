using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//[AddComponentMenu("Custom/TestComponent")]

public class TestEditer : MonoBehaviour
{
    /*public Transform transform;
    public Animator animator;

    [Header("Commons")] //제목
    public MeshRenderer[] renderers;
    public GameObject hat;
    public Transform firePoint;

    // 필드 사이에 여백
    [Space(20f)]
    public int attackPoint;
    public float attackSpeed;
    public float attackTime;

    [Space(10f)]
    public float attackSpeedMultiplier;
    public float attackTimeMultiplier;

    //변수의 값을 슬라이더 형식으로 표시하고 최소 및 최대 값을 정해주는 속성
    [Range(0f,5f)]
    public float resizeAmaunt;

    // 문자열을 작성하기 편하도록 라인의 수를 늘려주는 속성
    // 표시만 3줄이고 더 작성 가능함
    [Multiline(3)]
    public string text;

    // 문자열을 작성할 때, 라인 수의 최소크기와 최대 크기를 설정해주는 속성
    [TextArea(3,5)]
    public string text2;

    //private 변수를 인스펙터에 표시하는 속성
    [SerializeField]    // 변수 한개당 한개씩 필요
    Transform transpo;

    // 해당변수 위에 커서를 올릴 경우 나타나는 툴팁 설정하는 속성
    [Tooltip("발사 지점")]  // 변수 한개당 한개씩 필요
    public Vector3 firePos;

    // public 필드를 인스펙터에서 숨기는 속성
    [HideInInspector]   // 인스펙터창에서는 접근하기 싫고 다른 스크립트에서 접근하고 싶을 때 사용
    public float fireSpeed;

    */
}